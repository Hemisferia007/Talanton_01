fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
sqlalchemy>=2.0
python-multipart>=0.0.9
httpx>=0.27

# Capa de adquisición. Opcional para levantar la web y correr los tests;
# necesaria para la ingesta real (parseo adaptativo y sesiones stealth).
# Después de instalarla, correr: scrapling install
scrapling[fetchers]>=0.2
